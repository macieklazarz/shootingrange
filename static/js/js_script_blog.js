function loadfields(){
	if (getCookie('imie') == 'hide'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "imie"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).hide();
		}
	}
	else if (getCookie('imie') == 'display'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "imie"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).show();
		}
	}


	if (getCookie('nazwisko') == 'hide'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "nazwisko"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).hide();
		}
	}
	else if (getCookie('nazwisko') == 'display'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "nazwisko"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).show();
		}
	}


	if (getCookie('email') == 'hide'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "email"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).hide();
		}
	}
	else if (getCookie('email') == 'display'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "email"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).show();
		}
	}


	if (getCookie('licencja') == 'hide'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "licencja"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).hide();
		}
	}
	else if (getCookie('licencja') == 'display'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "licencja"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).show();
		}
	}

	if (getCookie('klub') == 'hide'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "klub"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).hide();
		}
	}
	else if (getCookie('klub') == 'display'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "klub"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).show();
		}
	}
	if (getCookie('klasasedziego') == 'hide'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "klasasedziego"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).hide();
		}
	}
	else if (getCookie('klasasedziego') == 'display'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "klasasedziego"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).show();
		}
	}
	if (getCookie('dataurodzenia') == 'hide'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "dataurodzenia"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).hide();
		}
	}
	else if (getCookie('dataurodzenia') == 'display'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "dataurodzenia"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).show();
		}
	}
	if (getCookie('czlonekstowarzyszenia') == 'hide'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "czlonekstowarzyszenia"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).hide();
		}
	}
	else if (getCookie('czlonekstowarzyszenia') == 'display'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "czlonekstowarzyszenia"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).show();
		}
	}


	if (getCookie('pesel') == 'hide'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "pesel"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).hide();
		}
	}
	else if (getCookie('pesel') == 'display'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "pesel"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).show();
		}
	}


	if (getCookie('nrtelefonu') == 'hide'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "nrtelefonu"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).hide();
		}
	}
	else if (getCookie('nrtelefonu') == 'display'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "nrtelefonu"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).show();
		}
	}



	if (getCookie('adres') == 'hide'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "adres"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).hide();
		}
	}
	else if (getCookie('adres') == 'display'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "adres"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).show();
		}
	}


	if (getCookie('kodpocztowy') == 'hide'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "kodpocztowy"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).hide();
		}
	}
	else if (getCookie('kodpocztowy') == 'display'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "kodpocztowy"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).show();
		}
	}
	if (getCookie('miejscowosc') == 'hide'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "miejscowosc"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).hide();
		}
	}
	else if (getCookie('miejscowosc') == 'display'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "miejscowosc"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).show();
		}
	}


	if (getCookie('imieojca') == 'hide'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "imieojca"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).hide();
		}
	}
	else if (getCookie('imieojca') == 'display'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "imieojca"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).show();
		}
	}

	if (getCookie('miejsceurodzenia') == 'hide'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "miejsceurodzenia"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).hide();
		}
	}
	else if (getCookie('miejsceurodzenia') == 'display'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "miejsceurodzenia"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).show();
		}
	}

	if (getCookie('numerpatentu') == 'hide'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "numerpatentu"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).hide();
		}
	}
	else if (getCookie('numerpatentu') == 'display'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "numerpatentu"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).show();
		}
	}

	if (getCookie('oplata') == 'hide'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "oplata"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).hide();
		}
	}
	else if (getCookie('oplata') == 'display'){
		// console.log('jest hide');

		const classes = [];
		for (const elem of document.querySelectorAll('[id $= "oplata"]')) {

			classes.push(elem);
		}
		for (const elem of classes){
			$(elem).show();
		}
	}

}

function sort_load(){
	setTimeout(function (){
  
  loadfields()
            
	}, 1000)
}

function setCookie(cname, cvalue) {

  document.cookie = cname + "=" + cvalue + ";";


}



function getCookie(cname) {
  let name = cname + "=";
  let decodedCookie = decodeURIComponent(document.cookie);
  let ca = decodedCookie.split(';');
  for(let i = 0; i <ca.length; i++) {
    let c = ca[i];
    while (c.charAt(0) == ' ') {
      c = c.substring(1);
    }
    if (c.indexOf(name) == 0) {
      return c.substring(name.length, c.length);
    }
  }
  return "";
}



function HideImie(){
	// document.cookie = "username=John Start";
	// setCookie('c1','v1');

	// if (getCookie('c1') == 'v1'){
	// 	console.log('jest');

	// }
	// setCookie('c2','v2');
	// console.log(getCookie('c1'));
	// console.log(getCookie('c2'));

	// setCookie('c1','v3');
	// setCookie('c2','v4');
	// console.log(getCookie('c1'));
	// console.log(getCookie('c2'));
	// console.log(document.cookie);
			
	const classes = [];

	for (const elem of document.querySelectorAll('[id $= "imie"]')) {

		classes.push(elem);
	}
			// console.log(classes);




	for (const elem of classes){

		if (elem.style.display == "none"){
			$(elem).show();
			setCookie('imie','display');

			// console.log(elem.style.display);
		} else {
			$(elem).hide();
			setCookie('imie','hide');
			// var cook = document.cookie + ' username=John Hide';
			// document.cookie = 'imie=jan';

		}
		// console.log(elem.classList);
	}

}

function HideNazwisko(){
			
	const classes = [];

	for (const elem of document.querySelectorAll('[id $= "nazwisko"]')) {

		classes.push(elem);
	}
			// console.log(classes);




	for (const elem of classes){

		if (elem.style.display == "none"){
			$(elem).show();
			setCookie('nazwisko','display');

			// console.log(elem.style.display);
		} else {
			$(elem).hide();
			setCookie('nazwisko','hide');
		}
		// console.log(elem.classList);
	}

}



function HideEmail(){
			
	const classes = [];

	for (const elem of document.querySelectorAll('[id $= "email"]')) {

		classes.push(elem);
	}
			// console.log(classes);




	for (const elem of classes){

		if (elem.style.display == "none"){
			$(elem).show();
			setCookie('email','display');

			// console.log(elem.style.display);
		} else {
			$(elem).hide();
			setCookie('email','hide');
		}
		// console.log(elem.classList);
	}

}



function HideLicencja(){
			
	const classes = [];

	for (const elem of document.querySelectorAll('[id $= "licencja"]')) {

		classes.push(elem);
	}
			// console.log(classes);




	for (const elem of classes){

		if (elem.style.display == "none"){
			$(elem).show();
			setCookie('licencja','display');

			// console.log(elem.style.display);
		} else {
			$(elem).hide();
			setCookie('licencja','hide');
		}
		// console.log(elem.classList);
	}

}

function HideKlub(){
			
	const classes = [];

	for (const elem of document.querySelectorAll('[id $= "klub"]')) {

		classes.push(elem);
	}
			// console.log(classes);




	for (const elem of classes){

		if (elem.style.display == "none"){
			$(elem).show();
			setCookie('klub','display');

			// console.log(elem.style.display);
		} else {
			$(elem).hide();
			setCookie('klub','hide');
		}
		// console.log(elem.classList);
	}

}

function HideKlasaSedziego(){
			
	const classes = [];

	for (const elem of document.querySelectorAll('[id $= "klasa_sedziego"]')) {

		classes.push(elem);
	}
			// console.log(classes);




	for (const elem of classes){

		if (elem.style.display == "none"){
			$(elem).show();
			setCookie('klasasedziego','display');

			// console.log(elem.style.display);
		} else {
			$(elem).hide();
			setCookie('klasasedziego','hide');
		}
		// console.log(elem.classList);
	}

}

function HideDataUrodzenia(){
			
	const classes = [];

	for (const elem of document.querySelectorAll('[id $= "data_urodzenia"]')) {

		classes.push(elem);
	}
			// console.log(classes);




	for (const elem of classes){

		if (elem.style.display == "none"){
			$(elem).show();
			setCookie('dataurodzenia','display');

			// console.log(elem.style.display);
		} else {
			$(elem).hide();
			setCookie('dataurodzenia','hide');
		}
		// console.log(elem.classList);
	}

}

function HideCzlonekStowarzyszenia(){
			
	const classes = [];

	for (const elem of document.querySelectorAll('[id $= "is_stowarzyszenie_member"]')) {

		classes.push(elem);
	}
			// console.log(classes);




	for (const elem of classes){

		if (elem.style.display == "none"){
			$(elem).show();
			setCookie('czlonekstowarzyszenia','display');

			// console.log(elem.style.display);
		} else {
			$(elem).hide();
			setCookie('czlonekstowarzyszenia','hide');
		}
		// console.log(elem.classList);
	}

}


function HidePESEL(){
			
	const classes = [];

	for (const elem of document.querySelectorAll('[id $= "pesel"]')) {

		classes.push(elem);
	}
			// console.log(classes);




	for (const elem of classes){

		if (elem.style.display == "none"){
			$(elem).show();
			setCookie('pesel','display');

			// console.log(elem.style.display);
		} else {
			$(elem).hide();
			setCookie('pesel','hide');
		}
		// console.log(elem.classList);
	}

}

function HideNrTelefonu(){
			
	const classes = [];

	for (const elem of document.querySelectorAll('[id $= "nr_telefonu"]')) {

		classes.push(elem);
	}
			// console.log(classes);




	for (const elem of classes){

		if (elem.style.display == "none"){
			$(elem).show();
			setCookie('nrtelefonu','display');

			// console.log(elem.style.display);
		} else {
			$(elem).hide();
			setCookie('nrtelefonu','hide');
		}
		// console.log(elem.classList);
	}

}

function HideAdres(){
			
	const classes = [];

	for (const elem of document.querySelectorAll('[id $= "adres"]')) {

		classes.push(elem);
	}
			// console.log(classes);




	for (const elem of classes){

		if (elem.style.display == "none"){
			$(elem).show();
			setCookie('adres','display');

			// console.log(elem.style.display);
		} else {
			$(elem).hide();
			setCookie('adres','hide');
		}
		// console.log(elem.classList);
	}

}


function HideKodPocztowy(){
			
	const classes = [];

	for (const elem of document.querySelectorAll('[id $= "kod_pocztowy"]')) {

		classes.push(elem);
	}
			// console.log(classes);




	for (const elem of classes){

		if (elem.style.display == "none"){
			$(elem).show();
			setCookie('kodpocztowy','display');

			// console.log(elem.style.display);
		} else {
			$(elem).hide();
			setCookie('kodpocztowy','hide');
		}
		// console.log(elem.classList);
	}

}


function HideMiejscowosc(){
			
	const classes = [];

	for (const elem of document.querySelectorAll('[id $= "miejscowosc"]')) {

		classes.push(elem);
	}
			// console.log(classes);




	for (const elem of classes){

		if (elem.style.display == "none"){
			$(elem).show();
			setCookie('miejscowosc','display');

			// console.log(elem.style.display);
		} else {
			$(elem).hide();
			setCookie('miejscowosc','hide');
		}
		// console.log(elem.classList);
	}

}


function HideImięOjca(){
			
	const classes = [];

	for (const elem of document.querySelectorAll('[id $= "imie_ojca"]')) {

		classes.push(elem);
	}
			// console.log(classes);




	for (const elem of classes){

		if (elem.style.display == "none"){
			$(elem).show();
			setCookie('imieojca','display');

			// console.log(elem.style.display);
		} else {
			$(elem).hide();
			setCookie('imieojca','hide');
		}
		// console.log(elem.classList);
	}

}


function HideMiejsceUrodzenia(){
			
	const classes = [];

	for (const elem of document.querySelectorAll('[id $= "miejsce_urodzenia"]')) {

		classes.push(elem);
	}
			// console.log(classes);




	for (const elem of classes){

		if (elem.style.display == "none"){
			$(elem).show();
			setCookie('miejsceurodzenia','display');

			// console.log(elem.style.display);
		} else {
			$(elem).hide();
			setCookie('miejsceurodzenia','hide');
		}
		// console.log(elem.classList);
	}

}


function HideOplata(){
			
	const classes = [];

	for (const elem of document.querySelectorAll('[id $= "oplata"]')) {

		classes.push(elem);
	}
			// console.log(classes);




	for (const elem of classes){

		if (elem.style.display == "none"){
			$(elem).show();
			setCookie('oplata','display');

			// console.log(elem.style.display);
		} else {
			$(elem).hide();
			setCookie('oplata','hide');
		}
		// console.log(elem.classList);
	}

}

function HideNumerPatentu(){
			
	const classes = [];

	for (const elem of document.querySelectorAll('[id $= "nr_patentu_strzeleckiego"]')) {

		classes.push(elem);
	}
			// console.log(classes);




	for (const elem of classes){

		if (elem.style.display == "none"){
			$(elem).show();
			setCookie('numerpatentu','display');

			// console.log(elem.style.display);
		} else {
			$(elem).hide();
			setCookie('numerpatentu','hide');
		}
		// console.log(elem.classList);
	}

};